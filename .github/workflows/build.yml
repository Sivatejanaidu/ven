name: Build-demo
run-name: This was triggered by @${{ github.actor }}
on:
 push:
    branches:
      - develop
jobs:
  build-job:
    runs-on: ubuntu-latest
  env:
    APP_NAME: "Demo-app"  
    steps:
    - name: build stage
      run: echo "Build stage completed with ${APP_NAME}"  